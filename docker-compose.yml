name: baby_monitor

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    # FIX: macでは/dev/video0がないため、エラーになる
    devices:
      - "/dev/video0:/dev/video0"
    ports:
      - "8000:5000"
    volumes:
      - .:/app
